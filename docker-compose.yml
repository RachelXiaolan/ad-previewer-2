services:
  ad-previewer:
    build: .
    container_name: ad-previewer
    restart: unless-stopped
    environment:
      NODE_ENV: production
      PORT: 3001
      CONFIG_FILE: /app/data/config.local.json
    ports:
      - "127.0.0.1:18081:3001"
    volumes:
      - ./data:/app/data
